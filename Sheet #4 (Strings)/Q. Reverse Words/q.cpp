#include <iostream>
#include <algorithm>
#include <string>
using namespace std;

int main() {
    string x;
    getline(cin, x);
    
    string y;
    int j = 0;

    for(int i = 0; i < x.size(); i++){
        if(x[i] == ' ' || i == x.size()-1){
            
        }
    }

    return 0;
}
